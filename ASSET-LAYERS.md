# 四套衣装素材与分层

版本：0.3.1。当前渲染入口为 SceneRenderer → LayeredCharacterRig，HuangLayers 是同一渲染器的黄玲琳素材适配部分。四套服装共同使用 SurfaceRig 身体动作和 ArmRig.TransformBound 肩腕绑定；键盘画面与输入共用 KeyboardModel。

本次接缝修复：CleanLayers 从透明部件图加载完整肩袖和眉毛，替代母图上的窄路径裁切。黄玲琳 classic/stage-limbs、朱慧月 stage-limbs 由内置 imagegen 依据原母图提取；朱慧月常服复用已有 casual-parts 的透明袖子。要求保留团子手、刺绣和袖口轮廓、排除头发/胸口/桌面；生成部件按肩点和掌心重新映射到运行坐标。肩点跟随身体同一变形，常服衣襟在肩袖前遮挡。黄玲琳眼部覆盖改为沿眼睛外轮廓的多边形，包含上睫毛尖；眉毛独立且完整。朱慧月 star-eyes-small 为局部缩小星形瞳孔版本，金色虹膜保持。

新增回归检查：固定其它动作状态，仅改变键盘敲击或鼠标位置，验证胸口衣襟像素不变。审阅仍须检查放大后的图层边缘和运动姿态，不能用输入检查数量替代美术验收。

## 黄玲琳

`assets/huang` 中每套包含 master、plate、ornaments、closed 四张透明图，1536×1024。母图来自用户此前指定的优化版：常服保留小撇嘴；打歌服将自然眉型及嘴型同步常服，保留服装、发型、团子手与红橙虹膜。

使用内置 imagegen 作局部编辑：提取透明背景；底板移除需独立动画的头饰、虹膜、眉毛、肩袖团子手及桌面键鼠；单独保留完整头饰；闭眼图只改变眼睛，不改变脸型、眉毛、嘴巴、服装。生成结果复制进项目后由运行时蒙版提取图层。运行时还有独立的左/右虹膜、眉毛、肩袖手、鼠标三种按下状态及五种表情覆盖层。

## 朱慧月

`assets/zhu` 保留 casual/stage 的 master、plate、parts、closed，1323×1189，以及共用 star-eyes。星形瞳孔经 imagegen 局部编辑，以黄玲琳细长白色菱形星瞳为形状参考，保持朱慧月金色虹膜与原脸型。两套服装左右眼复用同一个虹膜片，菜单头像同步此片。

头饰、流苏、眉毛、虹膜、眼睛闭合层、肩袖团子手、完整鼠标分别绑定。键盘由统一模型绘制，不再将按键高亮贴到另一种预绘键帽布局上。

## 验证与限制

使用最终 SceneRenderer 验证四套衣装，而非只检查输入状态。自动眨眼对自然和惊讶嘴部做像素不变检查；鼠标检查对应半边变暗、另一半不变及释放还原；逐个检查14键可见反馈。静态审阅包含四角鼠标姿态和全部表情。录制的离线视频使用预设轨迹，不作为实体输入或实际帧率证明。
